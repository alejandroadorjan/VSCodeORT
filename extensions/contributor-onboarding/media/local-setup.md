# Set Up Locally

A useful local setup is more than dependency installation:

* fork and clone the repository
* install the expected Node.js version
* install dependencies
* build Code - OSS
* launch the development version
* reproduce the issue before editing

For UI or multi-process behavior, identify the process you need to debug: renderer, extension host, search process, shared process, or Electron main process.
